# Task Contract

## Goal
Add a YouTube-inspired modern CSS gradient border and box-shadow glow effect to the final HTML dashboard generated by `EvaluateModel`.

## Acceptance Criteria
- The generated HTML dashboard includes a deliberate gradient-border/glow treatment on the main shell and chart cards.
- The effect is implemented with self-contained HTML/CSS only; no external CDN or remote asset is introduced.
- Existing dashboard interactivity and report generation still work.
- Automated tests cover the presence of the new visual-effect hooks in the generated HTML.
- A sample HTML report is generated and visually reviewed after the change.

## Non-Goals
- Reworking the data logic, chart metrics, or campaign-selection behavior.
- Recreating the video frame-by-frame with animation-heavy effects unless needed for the core visual style.

## Assumptions / Open Questions
- Assumption: the requested effect is the modern gradient border plus soft glow/box-shadow treatment indicated by the video title and visible YouTube preview frame: `Modern CSS Border & Box Shadow Gradient Effect | Old vs New Design`.
- Assumption: applying the effect to the report shell, KPI/cards, and control surfaces is the most useful interpretation for the final dashboard.
- Open question: none blocking for this slice.

# Strategic Plan
- Add a task-specific regression test for the new gradient-shell styling hooks.
- Update the dashboard HTML/CSS shell to apply the new border/glow effect without breaking the report structure.
- Generate and visually review the final HTML output, then document the result and any tradeoffs.

# Tactical Plan
- [x] Create the task file and record assumptions from the YouTube reference.
- [x] Add failing test coverage for the new gradient-effect markup/CSS hooks.
- [x] Implement the gradient-border/glow treatment in `evaluate_model.py`.
- [x] Run `pytest` and regenerate the HTML report.
- [x] Visually inspect the rendered report and refine if needed.
- [x] Update the task log, checklist, and final summary with evidence.

# Architecture Notes
- Public API remains unchanged:
  - `EvaluateModel(output_html_path: str | Path = "outputs/model_evaluation_report.html", seed: int | None = 42, ...) -> Path`
- Implementation scope:
  - Update only the HTML/CSS shell emitted by `EvaluateModel`.
  - Keep Plotly chart generation and client-side JS behavior unchanged unless required for visual fit.
- Visual strategy:
  - Use layered gradients, pseudo-element-free CSS where practical, and restrained glow/shadow treatment.
  - Keep the output self-contained and offline-friendly.
- Error handling:
  - No new runtime pathways expected; existing report generation should continue to fail loudly on existing validation errors.

# Test Plan

## Automated tests (what/where)
- `tests/test_evaluate_model.py`
  - Assert generated HTML contains the new gradient-shell class/hooks and CSS variables/selectors.
  - Retain existing checks for interactive controls and cards.

## Manual verification script
- Run: `python -m pytest`
- Run: `python evaluate_model.py`
- Open: `outputs/model_evaluation_report.html`
- Verify:
  - the page shell has a visible gradient border/glow treatment
  - chart cards feel visually consistent with the shell
  - controls and charts remain readable and interactive
  - no external asset dependency is required for styling

# Progress Log
- 2026-03-13: Intake completed; confirmed YouTube page access and inferred the requested effect from the video title and preview frame.
- 2026-03-13: Planner opened this task file and documented the slice contract, assumptions, and verification plan.
- 2026-03-13: QA added a failing regression test in `tests/test_evaluate_model.py` for the new gradient shell/card hooks.
- 2026-03-13: Developer updated `evaluate_model.py` to add the dashboard-shell and surface-card visual treatment with layered gradients and glow shadows.
- 2026-03-13: Build/Verify passed with `python -m pytest tests\test_evaluate_model.py -q` (19 passed) and `python evaluate_model.py` (report regenerated).
- 2026-03-13: Visual QA reviewed the rendered report in a local browser session served over `http://127.0.0.1:8765/model_evaluation_report.html`; the gradient border/glow effect was visible and interactive controls remained usable.
- 2026-03-13: Reviewer approved the slice with no blocking issues; residual note: browser console showed only a missing `favicon.ico` request from the local ad-hoc HTTP server.

# Final Summary
Completed. The final HTML dashboard now uses a YouTube-inspired modern gradient border and glow effect on the main report shell and card surfaces, implemented entirely in self-contained CSS. Regression coverage was added for the new styling hooks, tests passed, the report was regenerated successfully, and the rendered dashboard was visually reviewed in-browser.
